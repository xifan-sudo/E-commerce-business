# E-commerce-business
An E-commerce business sys

## Data processing

The repository now includes a small data processing utility:

- `process_data(values)` in `/tmp/workspace/xifan-sudo/E-commerce-business/data_processing.py`
- Ignores `None` entries
- Accepts numeric values (`int`, `float`)
- Returns a summary with `count`, `sum`, and `average`
